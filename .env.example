# Copie este arquivo para .env.local (nunca commitado, ver .gitignore)
# Use as credenciais do projeto Supabase de HOMOLOGACAO/DEV, nunca as de producao.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Só necessária pra rodar scripts/seed-admin.js — combine o valor com o time,
# nunca preencha aqui nem commite.
SEED_ADMIN_PASSWORD=
