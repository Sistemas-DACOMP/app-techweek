# Security Finding (spec section 22)

```
SECURITY FINDING

Severity:
Evidence:
Impact:
Affected component:
Recommended remediation:
Validation:
```

Severity scale: LOW / MEDIUM / HIGH / CRITICAL. `Evidence` must be FACT (reproduced), never a bare assumption — if unverified, say `UNKNOWN — needs reproduction`, don't upgrade it to a finding silently.
