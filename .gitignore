# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment / secrets — nunca versionar
.env
.env.*
!.env.example
*.pem
*.key
*.p12
credentials.json
service-account*.json

# Notas/contexto de sessão com o Claude — local, nunca versionado
claude-context/

# Git worktrees de agente (isolamento por tarefa) — cópia local completa do repo,
# nunca versionar (encontrado sem ignore em 2026-09-22, .claude/worktrees/kan45 aparecia
# como untracked em vez de ignorado)
.claude/worktrees/

# Config local do Maestri (roles/terminais desta máquina) — mesma categoria de .claude/,
# nunca versionar (encontrado sem ignore em 2026-09-22)
.maestri/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
